import { colors } from '@/theme/colors';

export const shadows = {
  bottomNavigation: {
    color: colors.shadow,
    elevation: 8,
    offsetY: -4,
    opacity: 0.04,
    radius: 6,
  },
} as const;
